import { IDBPDatabase, IDBPTransaction } from "idb";
import { Bound, KeyValueStore } from "@canvas-js/okra";
export declare class IDBStore implements KeyValueStore {
    readonly db: IDBPDatabase;
    readonly storeName: string;
    private readonly log;
    txn: IDBPTransaction<unknown, [string], IDBTransactionMode> | null;
    constructor(db: IDBPDatabase, storeName: string);
    write<T>(callback: () => Promise<T>): Promise<T | undefined>;
    read<T>(callback: () => Promise<T>): Promise<T>;
    get(key: Uint8Array): Promise<Uint8Array | null>;
    set(key: Uint8Array, value: Uint8Array): Promise<void>;
    delete(key: Uint8Array): Promise<void>;
    entries(lowerBound?: Bound<Uint8Array> | null, upperBound?: Bound<Uint8Array> | null, { reverse }?: {
        reverse?: boolean;
    }): AsyncIterableIterator<[Uint8Array, Uint8Array]>;
}
//# sourceMappingURL=store.d.ts.map