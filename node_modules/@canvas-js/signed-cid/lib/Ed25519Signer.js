var __classPrivateFieldSet = (this && this.__classPrivateFieldSet) || function (receiver, state, value, kind, f) {
    if (kind === "m") throw new TypeError("Private method is not writable");
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a setter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
    return (kind === "a" ? f.call(receiver, value) : f ? f.value = value : state.set(receiver, value)), value;
};
var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var _Ed25519Signer_privateKey;
import { ed25519 } from "@noble/curves/ed25519";
import { varint } from "multiformats";
import { base58btc } from "multiformats/bases/base58";
import { getCID } from "./cid.js";
export class Ed25519Signer {
    /**
     * @param privateKey 32-byte ed25519 private key
     */
    constructor(privateKey = ed25519.utils.randomPrivateKey()) {
        _Ed25519Signer_privateKey.set(this, void 0);
        const encodingLength = varint.encodingLength(Ed25519Signer.code);
        const publicKey = ed25519.getPublicKey(privateKey);
        const bytes = new Uint8Array(encodingLength + publicKey.byteLength);
        varint.encodeTo(Ed25519Signer.code, bytes, 0);
        bytes.set(publicKey, encodingLength);
        this.uri = `did:key:${base58btc.encode(bytes)}`;
        __classPrivateFieldSet(this, _Ed25519Signer_privateKey, privateKey, "f");
    }
    sign(value, options = {}) {
        const cid = getCID(value, options);
        const signature = ed25519.sign(cid.bytes, __classPrivateFieldGet(this, _Ed25519Signer_privateKey, "f"));
        return { publicKey: this.uri, signature, cid };
    }
    export() {
        return { type: "ed25519", privateKey: __classPrivateFieldGet(this, _Ed25519Signer_privateKey, "f") };
    }
}
_Ed25519Signer_privateKey = new WeakMap();
Ed25519Signer.type = "ed25519";
Ed25519Signer.code = 0xed;
