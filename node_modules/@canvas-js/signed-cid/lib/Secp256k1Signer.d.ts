import type { Signature, Signer } from "@canvas-js/interfaces";
import { Codec } from "./codecs.js";
import { Digest } from "./digests.js";
export declare class Secp256k1Signer<T = any> implements Signer<T> {
    #private;
    static type: "secp256k1";
    static code: number;
    readonly uri: string;
    /**
     * @param privateKey 33-byte secp256k1 private key
     */
    constructor(privateKey?: Uint8Array);
    sign(value: T, options?: {
        codec?: string | Codec;
        digest?: string | Digest;
    }): Signature;
    export(): {
        type: "secp256k1";
        privateKey: Uint8Array;
    };
}
