export type Codec = {
    name: string;
    code: number;
    encode: (value: any) => Iterable<Uint8Array>;
};
export declare const codecs: Codec[];
export declare const defaultCodec = "dag-cbor";
export declare function getCodec(options: {
    codec?: string | Codec;
}): Codec;
