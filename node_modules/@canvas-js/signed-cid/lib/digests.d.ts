export type Digest = {
    name: string;
    code: number;
    digest: (iter: Iterable<Uint8Array>) => Uint8Array;
};
export declare const digests: Digest[];
export declare const defaultDigest = "sha2-256";
export declare function getDigest(options: {
    digest?: string | Digest;
}): Digest;
