export const encodeKey = (key) => key ?? new Uint8Array([]);
export function encodeNode({ level, key, hash, value }) {
    if (value === undefined) {
        return { level, key: encodeKey(key), hash };
    }
    else {
        return { level, key: encodeKey(key), hash, value };
    }
}
export const decodeKey = (key) => (key.length === 0 ? null : key);
export function decodeNode({ level, key, hash, value }) {
    if (value === undefined) {
        return { level, key: decodeKey(key), hash };
    }
    else {
        return { level, key: decodeKey(key), hash, value };
    }
}
