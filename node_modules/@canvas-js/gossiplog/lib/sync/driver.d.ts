import { Source, Target } from "@canvas-js/okra";
export declare class Driver {
    private readonly source;
    private readonly target;
    private readonly log;
    constructor(topic: string, source: Source, target: Target);
    sync(): AsyncGenerator<[key: Uint8Array, value: Uint8Array], void, unknown>;
    private syncRoot;
    private syncNode;
}
