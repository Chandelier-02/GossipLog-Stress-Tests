import { NodeStore } from "./store.js";
import { Metadata, Node, KeyValueStore } from "./interface.js";
export declare class Builder extends NodeStore {
    static open(store: KeyValueStore, metadata?: Metadata): Promise<Builder>;
    private nodeCount;
    set(key: Uint8Array, value: Uint8Array): Promise<void>;
    finalize(): Promise<Node>;
    private buildLevel;
}
//# sourceMappingURL=builder.d.ts.map